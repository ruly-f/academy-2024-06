version: 2

models:
  - name: dim_funcionarios
    description: Dimensão de funcionários da Northwind.
    columns:
      - name: pk_funcionario
        description: Chave primária da dimensão funcionários.
        tests:
          - unique
          - not_null
